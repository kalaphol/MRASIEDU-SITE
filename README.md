Eminentmarks — Simple static site

How to preview:

1. Open `index.html` in a browser (double-click or use a local server).
2. Click a product to open its detail page; use the Call button to call.

Notes:
- Replace the SVG files in `images/` with your real product photos. Keep the same filenames or update `index.html` and `script.js` accordingly.
- To change the phone number, edit the `tel:` links in `index.html` and `product.html` (and `script.js`).
- Add your brand logo to `images/eminentmarks-logo.png`. The site header and footer reference that filename; drop the provided logo there and refresh the page.
